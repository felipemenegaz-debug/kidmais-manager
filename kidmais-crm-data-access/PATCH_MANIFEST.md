# PATCH MANIFEST — CRM Data Access

Copiar para a raiz do projeto Kidmais Manager:

- `lib/db/*` → `lib/db/*`
- `lib/clientes/repositories/*` → `lib/clientes/repositories/*`
- `database/migrations/*` → `database/migrations/*`
- usar `.env.example` somente como referência; credenciais reais ficam em `.env.local`

Dependências adicionais: `pg` e tipos TypeScript de `pg`.

Nenhum arquivo de `app/` ou `components/` é substituído nesta etapa.
